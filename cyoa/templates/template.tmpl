<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>{{.Title}}</title>
	</head>
	<body>
		<div>
			{{range .Story}} <div> {{ . }} </div>
			{{else}}<div><strong>no rows</strong></div>{{end}}
		</div>
        <div>
            {{range .Options}}<div>{{ . }}</div>
            {{else}}<div><strong>no rows</strong></div>{{end}}
        </div>
	</body>
</html>